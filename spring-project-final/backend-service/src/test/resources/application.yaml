spring:
    main:
      allow-circular-references: true
    mongodb:
      port: 0
      database: test
      uri: mongodb://localhost


server:
  port: 9091
debug: true

logging:
  level:
    org:
      springframework:
        web: DEBUG
